---
layout: post
title: "ArchLinux"
date: 2025-07-11 09:00:00 -03:00
---

1. Automation Script for Arch Linux:
Create a file named setup_arch_linux.sh and add the following content:

bash
Copiar
Editar
#!/bin/bash

# Update the system
echo "Updating the system..."
sudo pacman -Syyu --noconfirm

# Install essential packages
echo "Installing essential packages..."
sudo pacman -Syu base-devel git gnupg gperf ccache lzip zlib curl python3 python-pip unzip openjdk11-jdk wget flex bison make clang lld llvm ncurses ninja libxml2 libxslt libxcrypt-compat repo perl dtc bc python-yaml --noconfirm

# Configure Git
echo "Configuring Git..."
git config --global core.editor /usr/bin/nano
git config --global user.email "mezaquegit@gmail.com"
git config --global user.name "Mezaque Silver"

# Install 'repo' repository tool
echo "Installing Repo..."
mkdir -p ~/.bin
curl https://storage.googleapis.com/git-repo-downloads/repo > ~/.bin/repo
chmod a+x ~/.bin/repo
echo 'export PATH=~/.bin:$PATH' >> ~/.zshrc
source ~/.zshrc

# Install Git LFS
echo "Installing Git LFS..."
sudo pacman -S git-lfs --noconfirm
git lfs install

# Install Zsh and Oh-My-Zsh
echo "Installing Zsh and Oh-My-Zsh..."
sudo pacman -S zsh --noconfirm
sh -c "$(wget -O- https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"

# Edit the /etc/passwd file to set Zsh as the default shell
echo "Editing /etc/passwd to set Zsh as default shell..."
sudo nano /etc/passwd

# Install ADB and Fastboot
echo "Installing ADB and Fastboot..."
sudo pacman -S adb fastboot --noconfirm

# Install packages required for compiling ROMs
echo "Installing packages for compiling ROMs..."
git clone --depth 1 git@github.com:akhilnarang/scripts scripts && cd scripts && bash setup/arch-manjaro.sh

# Install Python dependencies
echo "Installing Python dependencies..."
python3 -m pip install --user numpy

# Configure Ccache
echo "Configuring Ccache..."
sudo pacman -S ccache --noconfirm
export USE_CCACHE=1
export CCACHE_DIR=~/.ccache
ccache -M 40G

# Create directories for Android utilities
echo "Creating directories and installing Android utilities..."
mkdir AndroidDumps
cd AndroidDumps
git clone git@github.com:ShivamKumarJha/android_tools
cd android_tools && bash setup.sh

# Finalization
echo "Configuration finished on Arch Linux!"
How to Use the Script:
Create a new file named setup_arch_linux.sh in your terminal:

bash
Copiar
Editar
nano setup_arch_linux.sh
Paste the code above.

Make the script executable:

bash
Copiar
Editar
chmod +x setup_arch_linux.sh
Run the script:

bash
Copiar
Editar
./setup_arch_linux.sh
This script automates the entire process on Arch Linux, including the installation of essential packages, terminal configuration with Zsh, ADB, Git, and more. It also configures variables like ccache to improve the build performance.
